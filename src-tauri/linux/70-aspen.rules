# Grant the active local desktop user access to the VLFD board.
# Run before systemd's 73-seat-late.rules applies the uaccess ACL.
SUBSYSTEM=="usb", ENV{DEVTYPE}=="usb_device", ATTR{idVendor}=="2200", ATTR{idProduct}=="2008", TAG+="uaccess"
