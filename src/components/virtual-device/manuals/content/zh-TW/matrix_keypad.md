用於模擬掃描式矩陣鍵盤，而不是簡單的並行按鍵組。

## 方向一定要接對

- `ROW`：由設計驅動出來，只觀察
- `COL`：在按鍵命中時回驅給設計

## 按鍵閉合模型

- 設計通常會逐行掃描矩陣。
- 當檢測到某一行正被選中，並且對應位置的鍵被按下時，會把對應列驅動成有效電平。
- “有效電平”決定列線被拉高還是拉低。

## 適合除錯什麼

- 矩陣掃描控制器
- 鍵盤去抖前後的行為
- 行列接線順序
- 鍵值譯碼邏輯
