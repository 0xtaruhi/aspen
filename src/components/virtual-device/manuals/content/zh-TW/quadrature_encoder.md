用於向設計注入正交編碼器訊號，適合模擬旋鈕和帶按壓的編碼器。

## 輸出內容

- `A` / `B`：標準正交相位
- `SW`：可選按壓開關

如果關閉按壓開關引數，`SW` 槽位也會從器件定義裡移除。

## 旋轉模型

- 順時針和逆時針會產生相反的 A/B 相位順序。
- 這適合測試增量計數、方向判斷和選單旋轉輸入。
- 當前模型關注的是邏輯相位關係，不模擬真實機械編碼器抖動。

## 適合除錯什麼

- 編碼器相位譯碼
- 加減計數
- 方向反轉判斷
- 帶按壓旋鈕的人機互動控制邏輯
