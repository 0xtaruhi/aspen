用于观察单线 PWM 音频输出，并估算当前主频率和占空比。

## 可观察到什么

- 当前是否有活动输出
- 估算出来的频率
- 高电平占比
- 边沿数量

这些值都来自实时数据流采样，而不是直接读取 FPGA 内部寄存器。

## 这个频率是怎么来的

- 根据上升沿之间的样本间距估算周期。
- 然后结合当前数据流采样速率，换算出一个近似频率值。
- 因此它更接近“逻辑级频率探针”，不是高精度音频频谱仪。

## 适合调试什么

- 蜂鸣器有没有真正振荡
- 旋律发生器是否切到正确音高
- 占空比是否固定在预期范围
- PWM 输出是否卡死在一个常量电平
