用于向设计注入正交编码器信号，适合模拟旋钮和带按压的编码器。

## 输出内容

- `A` / `B`：标准正交相位
- `SW`：可选按压开关

如果关闭按压开关参数，`SW` 槽位也会从器件定义里移除。

## 旋转模型

- 顺时针和逆时针会产生相反的 A/B 相位顺序。
- 这适合测试增量计数、方向判断和菜单旋转输入。
- 当前模型关注的是逻辑相位关系，不模拟真实机械编码器抖动。

## 适合调试什么

- 编码器相位译码
- 加减计数
- 方向反转判断
- 带按压旋钮的人机交互控制逻辑
