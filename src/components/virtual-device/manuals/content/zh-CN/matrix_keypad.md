用于模拟扫描式矩阵键盘，而不是简单的并行按键组。

## 方向一定要接对

- `ROW`：由设计驱动出来，只观察
- `COL`：在按键命中时回驱给设计

## 按键闭合模型

- 设计通常会逐行扫描矩阵。
- 当检测到某一行正被选中，并且对应位置的键被按下时，会把对应列驱动成有效电平。
- “有效电平”决定列线被拉高还是拉低。

## 适合调试什么

- 矩阵扫描控制器
- 键盘去抖前后的行为
- 行列接线顺序
- 键值译码逻辑
