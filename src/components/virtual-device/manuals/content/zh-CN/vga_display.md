用于观察 VGA 风格的视频时序和像素总线，并把采样结果重建成屏幕图像。

## 信号

- 持续观察 `HSYNC`、`VSYNC` 和 RGB 总线。
- `HSYNC` 用于行边界。
- `VSYNC` 用于帧边界。
- RGB 总线用于提供像素颜色数据。

## 图像恢复方式

- 根据同步信号划分行和帧，再按顺序采样 RGB 总线来恢复图像。
- 在非标准像素时钟下，也会按当前采样到的时序和像素流重建画面。

## 参数

- “分辨率”参数定义图像重建时使用的目标像素阵列尺寸。
- “颜色模式”参数定义 RGB 总线各个位如何映射到颜色通道。
